import React from "react";

const JoinusPage = () => {
  return (
    <div>
      A simple 2 column Grid MUI element, Picture and text on the right, email
      form on the left
    </div>
  );
};

export default JoinusPage;
